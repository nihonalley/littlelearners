const fs=require("fs");const path=require("path");const ROOT=__dirname,DIST=path.join(ROOT,"dist"),ITEMS=["index.html","css","js","games","assets"];fs.rmSync(DIST,{recursive:true,force:true});fs.mkdirSync(DIST,{recursive:true});function copy(src,dst){const s=fs.statSync(src);if(s.isDirectory()){fs.mkdirSync(dst,{recursive:true});for(const item of fs.readdirSync(src))copy(path.join(src,item),path.join(dst,item));return}fs.copyFileSync(src,dst)}for(const item of ITEMS){const src=path.join(ROOT,item);if(fs.existsSync(src))copy(src,path.join(DIST,item))}console.log("Build complete: dist/");
